import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "تواصل | قهوة إيمان",
};

export default function ContactLayout({ children }: { children: React.ReactNode }) {
  return children;
}
